spring:
  application:
    name: devops
  datasource:
    url: jdbc:mysql://db:3306/mydb
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none